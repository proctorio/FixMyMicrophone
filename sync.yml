trigger:
  branches:
    include:
    - main

resources:
  repositories:
    - repository: templates
      type: git
      name: Plumbing/core-templates
    - repository: FixMyMicrophone
      type: git
      name: Open/FixMyMicrophone

stages:
- template: stages/sync/azure-sync.yml@templates  # Template reference
  parameters:
    checkout: FixMyMicrophone
